# Copy this file to .env and fill in your values
# Command: cp env-template.txt .env

# AI API Configuration
# Use either DeepSeek or OpenAI API key (DeepSeek is more cost-effective)

# DeepSeek API (Recommended - cheaper alternative)
# Get your API key from: https://platform.deepseek.com/api_keys
DEEPSEEK_API_KEY=sk-your-deepseek-api-key-here

# OR use OpenAI API (uncomment if using OpenAI instead)
# Get your API key from: https://platform.openai.com/api-keys
# OPENAI_API_KEY=sk-your-openai-key-here

# Server Configuration
PORT=3001

# CORS Configuration
# Add your production domain here (comma-separated for multiple origins)
ALLOWED_ORIGIN=http://localhost:3000,https://nycrenovationexperts.com

# Environment
NODE_ENV=development

# Admin Dashboard Password
# Change this to a strong password in production!
ADMIN_PASSWORD=your-strong-admin-password-here

# ===== Retailer Product Search (Phase 1: Tools & Materials) =====
# SerpAPI key for live Google Shopping results across Home Depot, Lowe's,
# IKEA, Amazon, Wayfair, etc. Without this, /api/product-search returns mock
# data so the UI is still exercisable.
# Get one at: https://serpapi.com/  (free tier: 100 searches/mo)
SERPAPI_KEY=

# ===== Affiliate / Referral (Phase 1: hybrid revenue layer) =====
# Optional. If set, product links to that retailer are rewritten so you earn
# commission on referred sales. Apply for each network separately:
# - Amazon Associates:        https://affiliate-program.amazon.com
# - Home Depot / Lowe's:      via Impact (https://impact.com) or CJ (https://cj.com)
# - Wayfair:                  via CJ
AMAZON_ASSOCIATE_TAG=
HOMEDEPOT_AFFILIATE_PREFIX=
LOWES_AFFILIATE_PREFIX=
WAYFAIR_AFFILIATE_PREFIX=

# ===== Supabase (quote submission storage) =====
# Project URL and keys from your Supabase project: Settings -> API
# - SUPABASE_URL: the Project URL (e.g. https://xxxxx.supabase.co)
# - SUPABASE_ANON_KEY: the "anon public" key (currently unused server-side
#   but kept here for future frontend-only reads if we add RLS-friendly
#   queries)
# - SUPABASE_SERVICE_ROLE_KEY: the "service_role secret" key. THIS BYPASSES
#   Row Level Security — guard it like an admin password. Server-side only,
#   never expose to the frontend.
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
